import 'package:flutter/material.dart';

class AreaList extends StatelessWidget {
  const AreaList({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(child: Scaffold(body: Text('data')));
  }
}
